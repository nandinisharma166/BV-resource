# Disable JDBC auto-configuration (because using MongoDB)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# Server configuration
server.port=8080
server.address=0.0.0.0

# MongoDB configuration (use environment variable for security)
# spring.data.mongodb.uri=mongodb+srv://BVresource:DiyaSharma000023@cluster0.v8xd2.mongodb.net/BVresource?retryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI}

# Enable CORS globally
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
